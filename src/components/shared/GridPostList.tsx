import { Models } from "appwrite"

interface GridPostListProps {
  posts: Models.Document[]
}

const GridPostList = ({}: GridPostListProps) => {
  return (
    <div>
      
    </div>
  )
}

export default GridPostList
